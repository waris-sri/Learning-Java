public class VariableScope {
    public static void main(String[] args) {

        // local = declared inside a method; can only be accessed inside it
        // global = declared outside a method; can be accessed from anywhere

        new DiceRoller();
    }
}